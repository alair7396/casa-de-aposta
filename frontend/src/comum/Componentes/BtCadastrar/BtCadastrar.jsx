import './BtCadastrar.css';

const BtCadastrar = ({ onClick }) => {
  return (
    <button className="btcadastrar" onClick={onClick}>
      Cadastrar
    </button>
  );
};

export default BtCadastrar;
